export const RedirectToLogin = {
    redirect: {
        destination: '/api/auth/login',
        permanent: false,
    }
}

export const RedirectToEditor = {
    redirect: {
        destination: '/editor',
        permanent: false,
    }
}
